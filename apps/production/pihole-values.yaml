apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
    name: pihole
    namespace: pihole
spec:
    chart:
        spec:
            version: ">=1.0.0"
    values:
        ingress:
            hosts:
                - pihole.martijnj.nl
        DNS1: "1.1.1.1"
        DNS2: "1.0.0.1"
